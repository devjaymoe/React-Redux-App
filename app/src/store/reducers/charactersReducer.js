const initialState = {
    characters: [],
    isFetching: false,
    error: ''
};

export const charactersReducer = ( state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};